= Layer 2 セカンダリネットワークの設定
:navtitle: Layer 2 セカンダリネットワーク

== 概要

本ガイドでは、クラスター全体の論理スイッチを介して `VirtualMachines` を接続するための `layer2` セカンダリネットワークの設定方法を説明します。

これを行うことで、`VirtualMachines` はデフォルトの Pod ネットワークをプライマリネットワークとして継続的に使用できます。これは、以下の条件が当てはまる場合に有効です。

* 同じ namespace 内の (ノードが異なる場合でも) VM 間に単純な接続性が必要な場合
* namespace 内の VM 間でネットワークの分離が不要な場合
* VM 間のネットワーク接続を複数の namespace にまたがって拡張する必要がない場合
* クラスター内のノードの物理ネットワークインターフェースへのアクセスが不要な場合
* インターネットからセカンダリネットワークへのイングレス接続をルーティングする必要がない場合
* 最後に、セカンダリネットワークから外部ネットワークへのアクセスが不要な場合

検証済みバージョン:

----
OpenShift 4.20,4.21
----

== 前提条件

* OpenShift Virtualization Operator がインストールされた OpenShift 4.20 以降のクラスター
* `NetworkAttachmentDefinition` リソースを作成するためのクラスター管理者権限
* CLI ツールのインストール: `oc`, `virtctl`
* Kubernetes ネットワークの概念に関する基本的な理解

== 手順

プロセスは以下の 3 つのステップで構成されます。

* <<creating_a_namespace,namespace の作成>>
* <<creating_a_nad,NetworkAttachmentDefinition の作成>>
* <<attaching_vms,VirtualMachines のネットワークへの接続>>
** <<creating_first_vm,最初の VirtualMachine の作成>>
** <<creating_second_vm,2 つ目の VirtualMachine の作成>>
* <<testing_secondary_network,セカンダリネットワークのテスト>>


== namespace の作成 [[creating_a_namespace]]

最初のステップとして、ネットワークを定義する `NetworkAttachmentDefinition` と、ネットワークに接続したい `VirtualMachines` を格納するための namespace を作成します (まだ未設定の場合)。この場合、特別なラベルは必要ありません。

YAML を定義することなく、`oc` バイナリを使用して簡単に新しい namespace を作成できます。

[source,bash,role=execute]
----
oc create namespace example-l2-secondary
----

新しい namespace が作成されたので、現在の kube context (現在作業中のクラスターと namespace) を新しい namespace (OpenShift 用語では project) に切り替えるのが最適です。

[source,bash,role=execute]
----
oc project example-l2-secondary
----

namespace が作成され、現在の context が設定されたら、<<creating_a_nad,NetworkAttachmentDefinition の作成>> に進みます。

== NetworkAttachmentDefinition の作成 [[creating_a_nad]]

2 番目のステップは、namespace 内に `layer2` ネットワークを定義する `NetworkAttachmentDefinition` を作成することです。

`l2-network.nad.yaml` という名前の新しいファイルを作成し、以下の YAML 定義をファイルの内容としてコピー＆ペーストしてから、エディターを保存して閉じます。

[source,yaml]
----
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: l2-network
  namespace: example-l2-secondary
spec:
  config: |-
    {
            "cniVersion": "0.3.1",
            "name": "l2-network",
            "type": "ovn-k8s-cni-overlay",
            "topology":"layer2",
            "mtu": 1400,
            "netAttachDefName": "example-l2-secondary/l2-network"
    }
----

`oc apply` を使用して、OpenShift クラスター上の定義済みの namespace `example-l2-secondary` に `NetworkAttachmentDefinition` を適用します。

[source,bash,role=execute]
----
oc apply -f l2-network.nad.yaml
----

`NetworkAttachmentDefinition` が正常に作成されたことを確認します。次に進む前に、`l2-network` が `example-l2-secondary` namespace に存在している必要があります。

[source,bash,role=execute]
----
oc get net-attach-def l2-network -n example-l2-secondary
----

`l2-network` が作成されたら、<<attaching_vms,VirtualMachines の接続>> に進みます。


== VirtualMachines の接続 [[attaching_vms]]

次のステップは、`VirtualMachines` を作成し、作成したばかりの `layer2` ネットワークに接続することです。
これは、一般的な OpenShift (Kubernetes) のやり方に従い、`VirtualMachine` API オブジェクトを作成してクラスターに適用することで行います。

このセクションでは、どちらもセカンダリの `l2-network` に接続される 2 つの新しい VM を作成します。

=== 最初の VirtualMachine の作成 [[creating_first_vm]]

以下は、最初の VirtualMachine の YAML 定義です (主要なフィールドについては後述します)。
内容を `fedora-l2-secondary.vm.yaml` という新しいファイルにコピー＆ペーストしてください。

WARNING: この例の cloud-init パスワードはデモ目的のみのものです。本番環境では必ず強力で一意のパスワードまたは SSH 鍵認証を使用してください。

[source,yaml]
----
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: fedora-l2-secondary
  labels:
    app: fedora-l2-secondary
spec:
  dataVolumeTemplates:
  - metadata:
      name: fedora-l2-secondary-root
    spec:
      sourceRef:
        kind: DataSource
        name: fedora
        namespace: openshift-virtualization-os-images
      storage:
        resources:
          requests:
            storage: 30Gi
  instancetype:
    name: u1.large
  preference:
    name: fedora
  runStrategy: Always
  template:
    metadata:
      labels:
        app: fedora-l2-secondary
    spec:
      domain:
        devices:
          interfaces:
          - name: default
            masquerade: {}
          - name: secondary
            bridge: {}
        resources: {}
      terminationGracePeriodSeconds: 180
      networks:
      - name: default
        pod: {}
      - name: secondary
        multus:
          networkName: l2-network
      volumes:
      - name: fedora-l2-secondary-root
        dataVolume:
          name: fedora-l2-secondary-root
      - name: cloudinitdisk
        cloudInitNoCloud:
          userData: |
            #cloud-config
            user: fedora
            password: fedora
            chpasswd:
              expire: False
          networkData: |
            version: 2
            ethernets:
              enp2s0:
                addresses:
                - 192.168.20.10/24
----

[NOTE]
.上記の VM の例について
====
上記の例におけるフィールド (`spec.template.spec` に関連) について：

**domain.devices.interfaces** - インターフェース定義のリストを受け入れ、それぞれに 2 つのフィールドが定義されています。

* **name** - インターフェース名を指定します。ここでは `networks` で関連付けられたネットワーク名に合わせて `secondary` と命名しました。
* **bridge** または **masquerade** - セカンダリインターフェースは `bridge` を使用してハイパーバイザー Pod のインターフェースに接続されます。どちらのフィールドも空のオブジェクト (`{}`) を受け入れます。

**networks** - インターフェースの接続に使用するネットワークをリストアップする場所です。`interfaces` と同様に、各ネットワークには 2 つのフィールドがあります。

* **name** - `default` ネットワークと区別するために `secondary` というネットワーク名を適切に選択しました。
* **pod** または **multus** - デフォルト以外のネットワークを選択するには、フィールド名として `multus` を指定します。デフォルトの `pod` ネットワークは空のオブジェクトを受け入れますが、`multus` は `networkName` サブフィールドを必要とします。
** **networkName** (`multus` に必須) - 前述の手順で作成した `NetworkAttachmentDefinition` の名前である `l2-network` を指定します。

**volumes** - `fedora-l2-secondary-root` ディスクと、ユーザー資格情報やネットワークアドレスなどの VM 設定データを渡すために使用される `cloudinitdisk` の 2 つのボリュームが定義されています。
====

`oc create` を使用して、OpenShift クラスター上の現在の namespace (`example-l2-secondary`) に新しい VirtualMachine を作成できます。

[source,bash,role=execute]
----
oc create -f fedora-l2-secondary.vm.yaml
----

次に進む前に、`fedora-l2-secondary` VM が `example-l2-secondary` namespace で実行中であることを確認してください。`Provisioning` 状態の間は、コマンドを数回実行する必要がある場合があります。

[source,bash,role=execute]
----
oc get vm fedora-l2-secondary -n example-l2-secondary
----

[IMPORTANT]
====
.トラブルシューティング
`VirtualMachine` が `Pending` 状態で停止しているように見える場合、VM のスケジューリングを妨げている要因があります。考えられる要因は以下の通りです。

* クラスター上の基盤となる `DataVolume` のプロビジョニングに問題がある (ディスク容量不足など)。
* `l2-network` の `NetworkAttachmentDefinition` が存在しない。
* OpenShift クラスターにリソース制限がある。メモリ不足や vCPU 不足により、VM がスケジューリングできていない可能性。

問題の原因を特定するには、下位レベルのリソース (`vmi`、`datavolume`、`pvc` など) に対して `oc describe` または `oc get -o yaml` を使用してください。
エラーメッセージは、該当するリソースの Events (`oc describe`) または Status (`oc get -o yaml`) に記載されているはずです。
====

=== 2 つ目の VirtualMachine の作成 [[creating_second_vm]]

テストを行う前に、セカンダリネットワークを介した接続を確立するために 2 つ目の VM が必要です。

以下は、2 つ目の `VirtualMachine` の YAML ソース定義です。
提供された内容を `fedora-l2-secondary2.vm.yaml` という名前のファイルに保存してください。
最初の VM からの変更点は、名前、IP アドレス、ルートボリューム名のみであることに注意してください。

[source,yaml]
----
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: fedora-l2-secondary2
  labels:
    app: fedora-l2-secondary2
spec:
  dataVolumeTemplates:
  - metadata:
      name: fedora-l2-secondary2-root
    spec:
      sourceRef:
        kind: DataSource
        name: fedora
        namespace: openshift-virtualization-os-images
      storage:
        resources:
          requests:
            storage: 30Gi
  instancetype:
    name: u1.large
  preference:
    name: fedora
  runStrategy: Always
  template:
    metadata:
      labels:
        app: fedora-l2-secondary2
    spec:
      domain:
        devices:
          interfaces:
          - name: default
            masquerade: {}
          - name: secondary
            bridge: {}
        resources: {}
      terminationGracePeriodSeconds: 180
      networks:
      - name: default
        pod: {}
      - name: secondary
        multus:
          networkName: l2-network
      volumes:
      - name: fedora-l2-secondary2-root
        dataVolume:
          name: fedora-l2-secondary2-root
      - name: cloudinitdisk
        cloudInitNoCloud:
          userData: |
            #cloud-config
            user: fedora
            password: fedora
            chpasswd:
              expire: False
          networkData: |
            version: 2
            ethernets:
              enp2s0:
                addresses:
                - 192.168.20.20/24
----

OpenShift クラスター上の現在の namespace に `VirtualMachine` を適用します。

[source,bash,role=execute]
----
oc apply -f fedora-l2-secondary2.vm.yaml
----

`fedora-l2-secondary2` という名前の 2 つ目の VM が `example-l2-secondary` namespace で実行中であることを確認してください。<<testing_secondary_network,テスト>> を行う前に、両方の `VirtualMachines` が実行されている必要があります。

[source,bash,role=execute]
----
oc get vm fedora-l2-secondary2 -n example-l2-secondary
----


== セカンダリネットワークのテスト [[testing_secondary_network]]

セカンダリネットワークは `layer2` 専用であり、クラスターと namespace 内の仮想ネットワークとしてのみ存在するため、ある VM から別の VM への接続性をテストする必要があります。

テストを進めるには、`example-l2-secondary` namespace に以下のリソースが存在している必要があります。

* `NetworkAttachmentDefinition`: `l2-network`
* `VirtualMachine`: `fedora-l2-secondary` (セカンダリ NIC IP アドレス: 192.168.20.10)
* `VirtualMachine`: `fedora-l2-secondary2` (セカンダリ NIC IP アドレス: 192.168.20.20)

最も簡単なテスト方法は、一方の VM (または両方の VM) のコンソールに接続し、もう一方の VM に ping を送信することです。

`virtctl` コマンドを使用して最初の `VirtualMachine` のコンソールにアクセスし、**ユーザー名**:`fedora`、**パスワード**:`fedora` でログインします (ログインプロンプトが表示されない場合は `ENTER` を押してください)。

[source,bash,role=execute]
----
virtctl console fedora-l2-secondary
----

`VirtualMachine` の IP アドレスを確認します。次のコマンドを実行すると、`enp2s0` インターフェースに対して静的アドレス `192.168.20.10` が返されるはずです。

[source,bash]
----
ip addr show
----

最初の VM のコンソールから `fedora-l2-secondary2` (IP アドレス: `192.168.20.20`) VM に ping を送信します。応答があれば、セカンダリネットワークは機能しています (`CTRL+C` を押すと ping がキャンセルされます)。

[source,bash]
----
ping 192.168.20.20
----

VM コンソールを終了するには、`CTRL+]` を押します。

このチュートリアルで作成したすべてのリソースを削除するには、別の namespace に切り替えて `example-l2-secondary` namespace を削除するだけです。

[source,bash,role=execute]
----
oc project default
oc delete namespace example-l2-secondary
----

== まとめ

このチュートリアルでは、以下の内容を学びました。

* `ovn-k8s-cni-overlay` CNI プラグインを使用して `layer2` トポロジーの `NetworkAttachmentDefinition` を作成する方法
* プライマリ masquerade インターフェースとセカンダリ bridge インターフェースの両方を持つ VirtualMachines を設定する方法
* cloud-init `networkData` を使用してセカンダリネットワークに静的 IP アドレスを割り当てる方法
* 同じ OVN 管理の論理スイッチ上の VM 間の Layer 2 接続を確認する方法
* Linux bridge や localnet などの他のネットワークオプションと比較して、いつ Layer 2 セカンダリネットワークを使用すべきか判断する方法

== 関連リソース

* link:https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html/virtualization/networking#virt-connecting-vm-to-ovn-secondary-network[OpenShift Virtualization - OVN セカンダリネットワークへの VM の接続,window=_blank]
* link:https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html/multiple_networks/understanding-multiple-networks[OpenShift - 複数のネットワークの理解,window=_blank]