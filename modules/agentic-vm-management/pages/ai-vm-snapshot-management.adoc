= Agentic ワークフローによる VM スナップショット管理
:navtitle: VM スナップショット管理

このチュートリアルでは、4 つのエージェントスキル (`vm-snapshot-create`、`vm-snapshot-list`、`vm-snapshot-restore`、`vm-snapshot-delete`) を通じて自然言語コマンドを使用し、仮想マシンスナップショットの完全なライフサイクルを管理する方法を説明します。アップグレード前のスナップショットの作成、利用可能なスナップショットの一覧表示、アップグレード失敗後のリストア、リストアされた VM の検証、古いスナップショットのクリーンアップという現実的なシナリオを一通り行います。

== 前提条件

* xref:getting-started.adoc[Agentic VM 管理の開始] チュートリアルを完了していること (スキルパックがインストール済み、MCP サーバーが設定済み、クラスターへの接続が確認済み)
* OpenShift Virtualization がインストールされた OpenShift クラスター
* `KUBECONFIG` 環境変数が設定され、有効な kubeconfig ファイルを指していること
* 管理対象となる 1 つ以上の VM (作成するには xref:natural-language-vm-create.adoc[自然言語による仮想マシンの作成] を参照)
* CSI スナップショットをサポートするストレージバックエンド (ストレージプロビジョナー用の `VolumeSnapshotClass` が存在する必要があります)
* `VirtualMachineSnapshot` および `VirtualMachineRestore` リソースを作成、一覧表示、削除するための RBAC 権限

このチュートリアルで使用する namespace がまだ存在しない場合は、作成します。

[source,bash,role=execute]
----
oc create namespace my-vms
----

管理対象の VM がない場合は、IDE チャットで次のように入力し、`vm-create` スキルを使用して作成します。

----
Create a Fedora VM named database-01 in namespace my-vms
----

続行する前に、VM が存在することを確認します。

[source,bash,role=execute]
----
oc get vm -n my-vms
----

想定される出力:

----
NAME     AGE  STATUS  READY
database-01  5m  Running  True
----

ストレージバックエンドがスナップショットをサポートしていることを確認します。

[source,bash,role=execute]
----
oc get volumesnapshotclass
----

想定される出力 (ドライバー名およびクラス名は異なる場合があります):

----
NAME       DRIVER    DELETIONPOLICY  AGE
lvms-snapclass  topolvm.io  Delete      17d
----

`VolumeSnapshotClass` が返されない場合は、続行する前にクラスター管理者に連絡して設定してください。

== 動作の仕組み

4 つのスナップショットスキルは、スナップショットのライフサイクル全体をカバーします。各スキルは、データを変更または破棄する操作の前に、Human-in-the-loop (人間による確認) を強制します。

=== vm-snapshot-create スキル

`vm-snapshot-create` スキルは、複数のステップからなるワークフローに従います。

. **VM の検証とストレージの分析** -- エージェントは VM が存在することを確認し、ホットプラグされたボリューム (スナップショットをブロックします) をチェックし、StorageClass が CSI スナップショットをサポートしていることを検証して、一致する `VolumeSnapshotClass` を特定します。
. **VM の状態とゲストエージェントの確認** -- エージェントは VM が実行中か停止しているかを判別します。ストレージバックエンドがオンラインスナップショットをサポートしている場合、エージェントは QEMU ゲストエージェントが接続されているかどうかを報告します。ゲストエージェントが接続されていると、ファイルシステムのフリーズ/復帰 (freeze/thaw) を通じてアプリケーション整合性のあるスナップショットが可能になります。接続されていない場合、スナップショットはクラッシュ整合性となります。
. **ストレージの見積もりと設定の提示** -- エージェントはストレージ消費量を見積もり、適用される正確な YAML を表示して、明示的な確認を待ちます。
. **作成と監視** -- 確認後、エージェントは `VirtualMachineSnapshot` リソースを作成し、そのステータスが `Succeeded` または `Failed` になるまで監視します。

=== vm-snapshot-list スキル

`vm-snapshot-list` スキルは、確認を必要としない読み取り専用の操作です。 namespace 内のすべてのスナップショットを一覧表示し (オプションで VM 名によるフィルタリングが可能)、各スナップショットのステータス、経過時間、復元の準備状態を表示します。

=== vm-snapshot-restore スキル

`vm-snapshot-restore` スキルは、スナップショットスキルの中で最も厳格な安全モデルを適用します。

. **VM が停止していることの確認** -- 実行中の VM を復元するとデータの破損につながります。VM が実行中の場合、エージェントは最初に VM を停止するかキャンセルするかを提案します。
. **スナップショットの準備完了の確認** -- エージェントはスナップショットが存在し、`readyToUse: true` であることを確認します。
. **入力によるスナップショット名の確認** -- 確認のために正確なスナップショット名を入力する必要があります。一致しない場合、操作はキャンセルされます。
. **最終的な yes/cancel の確認** -- 入力による検証に合格した後、エージェントは実行前にもう一度確認します。
. **実行と監視** -- エージェントは `VirtualMachineRestore` リソースを作成し、リストアが完了するまで進捗を監視します。

=== vm-snapshot-delete スキル

`vm-snapshot-delete` スキルは、スナップショットを完全に削除してそのストレージを解放します。

. **スナップショットの存在確認** -- エージェントはスナップショットの詳細を取得し、同じ VM の他のスナップショットを一覧表示します。
. **最後のスナップショットの場合の警告** -- これが仮想マシンの唯一のスナップショットである場合、エージェントは復元ポイントが残らないという警告を表示します。
. **スナップショット名の入力による確認** -- 削除を確認するには、正確なスナップショット名を入力する必要があります。
. **削除とレポート** -- エージェントはスナップショットを削除し、残りのスナップショットとともに結果をレポートします。

== スナップショットの一貫性レベル

スナップショットの一貫性は、VM が実行中であるかどうか、および QEMU ゲストエージェントがアクティブであるかどうかによって異なります。

[cols="1,1,2",options="header"]
|===
| VM の状態 | ゲストエージェント | 一貫性レベル
| Stopped | N/A | オフラインスナップショット -- 完全に一貫性があり、正常なシャットダウンと同等
| Running | Connected | アプリケーション整合性あり -- スナップショットの作成中にゲストエージェントがファイルシステムを凍結
| Running | Not connected | クラッシュ整合性 -- 電源コードを抜いた状態と同等。使用可能ですが、リストア時にファイルシステムのリカバリーが必要になる場合があります
|===

実稼働 VM の場合、アプリケーション整合性のあるキャプチャーを確保するために、スナップショットを作成する前に QEMU ゲストエージェントをインストールしてください。Linux VM の場合:

[source,bash,role=execute]
----
sudo dnf install qemu-guest-agent
sudo systemctl enable --now qemu-guest-agent
----

Windows VM の場合は、ゲストエージェントが含まれ、Windows ワークロードのアプリケーション整合性スナップショットに対応する VSS (Volume Shadow Copy Service) をサポートしている VirtIO ドライバーパッケージをインストールします。

== 例シナリオ 1: アップグレード前のスナップショットの作成

このシナリオでは、VM 上のアプリケーションソフトウェアをアップグレードする前にスナップショットを作成する方法を説明します。スナップショットは、アップグレードが失敗した場合のロールバックポイントとして機能します。

. IDE チャットで、次のように入力します。
+
----
Create a snapshot of database-01 before I upgrade it
----
. エージェントが namespace を尋ねてきます。
+
----
Which namespace is database-01 in?
----
. namespace を指定します。
+
----
my-vms
----
. エージェントは VM を検証し、ストレージバックエンドを分析し、guest agent のステータスを確認して、確認用の設定を提示します。
+
----
## VM Snapshot - Review Configuration

**VM to Snapshot:**
- **Name**: `database-01`
- **Namespace**: `my-vms`
- **Status**: Running
- **Storage**: 30Gi

**Storage Backend Analysis:**
- **Storage Class**: `lvms-vg1`
- **CSI Driver**: `topolvm.io`
- **Online Snapshots**: Supported
- **VolumeSnapshotClass**: `lvms-snapclass`

**Guest Agent Status:**
- **QEMU Guest Agent**: Connected and running
- **Filesystem Freeze/Thaw**: Will be performed during snapshot
- **Expected Consistency**: Application-consistent (with freeze)

**Snapshot Details:**
- **Snapshot Name**: `database-01-snapshot-20260714-100000` (auto-generated)
- **Type**: Online snapshot
- **Estimated Storage**: ~30Gi (initial snapshot)

**YAML to be applied:**

  apiVersion: snapshot.kubevirt.io/v1beta1
  kind: VirtualMachineSnapshot
  metadata:
   name: database-01-snapshot-20260714-100000
   namespace: my-vms
  spec:
   source:
    apiGroup: kubevirt.io
    kind: VirtualMachine
    name: database-01

**Proceed with snapshot creation? (yes/no)**
----
. 設定を確認し、承認します。
+
----
yes
----
. エージェントはスナップショットを作成し、その進行状況を監視します。
+
----
Creating VM snapshot...
Snapshot `database-01-snapshot-20260714-100000` created for VM `database-01`

Snapshot creation in progress...
 Status: InProgress

Snapshot creation in progress...
 Status: Succeeded

## VM Snapshot Created Successfully

**VM**: `database-01` (namespace: `my-vms`)
**Snapshot**: `database-01-snapshot-20260714-100000`

### Snapshot Details
- **Name**: `database-01-snapshot-20260714-100000`
- **Status**: Ready
- **Created**: 2026-07-14 10:00:22 UTC
- **VM Status at Snapshot**: Running

### Snapshot Coordination (from status.indications)
- **Guest Agent Coordination**: Active
- **Filesystem Freeze/Thaw**: Performed during snapshot
- **Actual Consistency**: Application-consistent

### Next Steps

**To list all snapshots:**
"List snapshots for VM database-01"

**To restore from this snapshot:**
"Restore VM database-01 from snapshot database-01-snapshot-20260714-100000"

You can now safely upgrade the database. If the upgrade fails,
restore using the command above.
----

スナップショットが作成されたことを確認できます。

[source,bash,role=execute]
----
oc get virtualmachinesnapshot -n my-vms
----

----
NAME                   SOURCEVMNAME  PHASE    READYTOUSE  CREATIONTIMESTAMP      AGE
database-01-snapshot-20260714-100000   database-01  Succeeded  true     2026-07-14T10:00:22Z    2m
----

== サンプルシナリオ 2: スナップショットの一覧表示

1 つ以上のスナップショットを作成した後、`vm-snapshot-list` スキルを使用してそれらを表示できます。これは確認を必要としない読み取り専用の操作です。

. IDE チャットで、次のように入力します。
+
----
List snapshots for VM database-01 in namespace my-vms
----
. エージェントがクラスターにクエリーを実行し、結果を表示します。
+
----
## VM Snapshots

**Namespace**: `my-vms`
**VM**: `database-01`

### Available Snapshots

| Snapshot Name             | VM Name    | Status   | Created     | Age   | ReadyToUse |
|----------------------------------------|---------------|-------------|------------------|---------|------------|
| `database-01-snapshot-20260714-100000` | `database-01` | Succeeded  | 2026-07-14 10:00 | 30 min | true    |
| `database-01-weekly-backup`      | `database-01` | Succeeded  | 2026-07-07 02:00 | 7 days | true    |

**Total Snapshots**: 2

---

### Actions

**To restore from a snapshot:**
"Restore VM database-01 from snapshot <snapshot-name>"

**To delete a snapshot:**
"Delete snapshot <snapshot-name>"

**To create a new snapshot:**
"Create snapshot of VM database-01"
----

VM でフィルタリングせずに namespace 内のすべてのスナップショットを一覧表示するには、次のように入力します。

----
Show me all snapshots in namespace my-vms
----

== シナリオ例 3: アップグレード失敗後のリストア

アップグレードに失敗し、データベースが破損しました。アップグレード前のスナップショットにロールバックする必要があります。このシナリオでは、厳格な安全性ワークフローを備えた `vm-snapshot-restore` スキルについて説明します。

. IDE チャットで、次のように入力します。
+
----
The database upgrade failed. Restore database-01 from snapshot database-01-snapshot-20260714-100000
----
. エージェントが namespace を尋ねます。
+
----
Which namespace is database-01 in?
----
. namespace を指定します。
+
----
my-vms
----
. エージェントは VM が実行中であることを確認し、最初に停止する必要があることを警告します。
+
----
## VM Must Be Stopped Before Restore

**VM**: `database-01` (namespace: `my-vms`)
**Status**: Running

**Safety Requirement**: VMs must be stopped before restore to prevent
data corruption.

**Options:**
1. "stop-and-restore" - Stop the VM first, then restore from snapshot
2. "cancel" - Cancel restore operation

How would you like to proceed?
----
. 停止して復元することを選択します。
+
----
stop-and-restore
----
. エージェントは VM を停止し、スナップショットが存在して準備が完了していることを確認した上で、テキスト入力による確認を伴うデータ損失の警告を表示します。
+
----
## VM RESTORE - Data Loss Warning

THIS WILL REPLACE CURRENT VM STATE WITH SNAPSHOT DATA

### What Will Happen

**VM to Restore**: `database-01` (namespace: `my-vms`)
**Snapshot to Restore From**: `database-01-snapshot-20260714-100000`

**Current VM State** (WILL BE LOST):
- **Last Modified**: 2026-07-14 10:35:00 UTC
- **Changes Since Snapshot**: ALL changes made after
 2026-07-14 10:00:22 UTC WILL BE PERMANENTLY LOST

**Snapshot State** (WILL BE RESTORED):
- **Created**: 2026-07-14 10:00:22 UTC
- **Age**: 35 minutes

**Time Range of Data Loss**:
- ALL CHANGES in the last 35 minutes WILL BE LOST

### What Will Be Restored
- VM configuration (from snapshot time)
- Disk data (from snapshot time)

### What Will Be Lost
- ALL disk changes made after 2026-07-14 10:00:22 UTC
- ALL configuration changes made after 2026-07-14 10:00:22 UTC

---

CRITICAL: This restore is permanent. Current VM state cannot be
recovered unless you create a snapshot now.

**To proceed with restore, type the snapshot name exactly as shown:**

Type `database-01-snapshot-20260714-100000` to confirm: _____
----
. 正確なスナップショット名を入力します。
+
----
database-01-snapshot-20260714-100000
----
. エージェントは入力検証に合格したことを確認し、最終確認を求めます。
+
----
## Typed Verification Passed

**Confirmation received for snapshot**: `database-01-snapshot-20260714-100000`

### Ready to Restore

**VM**: `database-01` (namespace: `my-vms`)
**From Snapshot**: `database-01-snapshot-20260714-100000`

**Impact**:
- Current VM state will be replaced with snapshot state
- All changes in the last 35 minutes will be permanently lost

---

**Proceed with VM restore? This action cannot be undone.**
- Type "yes" to execute restore
- Type "cancel" to abort

Your choice: _____
----
. 確認を入力します。
+
----
yes
----
. エージェントは復元を実行し、進捗状況を監視します。
+
----
Restoring VM from snapshot...
This may take several minutes...

## VM Restored Successfully

**VM**: `database-01` (namespace: `my-vms`)
**Restored From**: Snapshot `database-01-snapshot-20260714-100000`

### Restore Details
- **Snapshot Created**: 2026-07-14 10:00:22 UTC
- **Restore Completed**: 2026-07-14 10:37:15 UTC
- **VM Status**: Stopped (ready to start)

### Data Loss Confirmation
- All changes made after 2026-07-14 10:00:22 UTC have been lost

### Next Steps

**To start the restored VM:**
"Start VM database-01 in namespace my-vms"
----

復元の完了後、VM を起動して正常であることを確認します。

[source,bash,role=execute]
----
oc get vm database-01 -n my-vms
----

----
NAME     AGE  STATUS  READY
database-01  45m  Stopped  False
----

IDE チャットで次のように入力し、`vm-lifecycle-manager` スキルを使用して VM を起動します。

----
Start VM database-01 in namespace my-vms
----

または直接起動します。

[source,bash,role=execute]
----
virtctl start database-01 -n my-vms
----

== サンプルシナリオ 4: 古いスナップショットの削除

リストアと検証が正常に完了したら、ストレージを解放するために古いスナップショットを削除できます。このシナリオでは、`vm-snapshot-delete` スキルについて説明します。

. IDE チャットで、次のように入力します。
+
----
Delete snapshot database-01-weekly-backup in namespace my-vms
----
. エージェントはスナップショットの詳細を取得し、同じ VM の他のスナップショットを一覧表示して、削除の確認画面を表示します。
+
----
## Snapshot Deletion - Review

**Snapshot to Delete**: `database-01-weekly-backup`

### Snapshot Details
- **Snapshot Name**: `database-01-weekly-backup`
- **VM**: `database-01`
- **Namespace**: `my-vms`
- **Created**: 2026-07-07 02:00:00 UTC
- **Age**: 7 days
- **Status**: Succeeded

### Impact of Deletion
- Snapshot will be permanently deleted
- This recovery point will be lost
- Cannot restore VM to this snapshot state after deletion
- Storage will be freed

### Recovery Impact

**Available snapshots for VM `database-01`:**
- `database-01-snapshot-20260714-100000` (created 2026-07-14 10:00)

---

**Type the snapshot name `database-01-weekly-backup` to confirm deletion
(cannot be undone):** _____
----
. 正確なスナップショット名を入力します。
+
----
database-01-weekly-backup
----
. エージェントはスナップショットを削除し、結果を報告します。
+
----
Deleting snapshot...
Snapshot `database-01-weekly-backup` deleted

## Snapshot Deleted Successfully

**Snapshot**: `database-01-weekly-backup`
(VM: `database-01`, namespace: `my-vms`)

### Deletion Summary
- Snapshot permanently deleted
- Storage freed
- Recovery point removed

### Remaining Snapshots for VM `database-01`
- `database-01-snapshot-20260714-100000` (created 2026-07-14 10:00)

This snapshot is still available for recovery.
----

スナップショットが削除されたことを確認できます。

[source,bash,role=execute]
----
oc get virtualmachinesnapshot -n my-vms
----

----
NAME                   SOURCEVMNAME  PHASE    READYTOUSE  CREATIONTIMESTAMP      AGE
database-01-snapshot-20260714-100000   database-01  Succeeded  true     2026-07-14T10:00:22Z    40m
----

== シナリオ例 5: 最後のスナップショットに対する警告

VM に残っている唯一のスナップショットを削除する場合、エージェントは追加の警告を発行します。

. IDE チャットで、次のように入力します。
+
----
Delete snapshot database-01-snapshot-20260714-100000 in namespace my-vms
----
. エージェントはこれが最後のスナップショットであることを検出し、警告します。
+
----
## Snapshot Deletion - Review

**Snapshot to Delete**: `database-01-snapshot-20260714-100000`

### Snapshot Details
- **Snapshot Name**: `database-01-snapshot-20260714-100000`
- **VM**: `database-01`
- **Namespace**: `my-vms`
- **Created**: 2026-07-14 10:00:22 UTC
- **Age**: 45 minutes
- **Status**: Succeeded

### Impact of Deletion
- Snapshot will be permanently deleted
- This recovery point will be lost
- Storage will be freed

WARNING: This is the ONLY snapshot for VM `database-01`. After deletion,
no snapshot recovery points will exist.

---

**Type the snapshot name `database-01-snapshot-20260714-100000` to confirm
deletion (cannot be undone):** _____
----

セーフティネットとしてこのスナップショットを保持したい場合は、正確なスナップショット名以外の文字を入力するか、チャットプロンプトを閉じます。エージェントは削除をキャンセルし、スナップショットを保持します。

== エンドツーエンドのシナリオ: アップグレード前のバックアップとリカバリー

本セクションでは、上記のシナリオで実践した一連のワークフローをまとめています。本番環境の VM でアップグレードを実行する際の参照チェックリストとして使用してください。

. **アップグレード前のスナップショットを作成する** -- アップグレードを開始する前に、ロールバックポイントとしてスナップショットを作成します。
+
----
Create a snapshot of database-01 in namespace my-vms
----
. **アップグレードを実行する** -- アップグレード手順を実行します (パッケージのインストール、設定の更新、移行の実行など)。
. **アップグレードを検証する** -- アプリケーションをテストして、アップグレードが成功したことを確認します。
. **アップグレードが成功した場合** -- ストレージを解放するために、アップグレード前のスナップショットを削除します。
+
----
Delete snapshot database-01-snapshot-20260714-100000 in namespace my-vms
----
. **アップグレードに失敗した場合** -- アップグレード前のスナップショットから VM を復元します。
+
----
Restore database-01 from snapshot database-01-snapshot-20260714-100000
----
. **復元した VM の起動** -- 復元が完了したら、VM を起動して正常であることを確認します。
+
----
Start VM database-01 in namespace my-vms
----
. **古いスナップショットのクリーンアップ** -- 復元した VM が正常であることを確認した後、不要になったスナップショットを削除します。
+
----
List snapshots for VM database-01 in namespace my-vms
----

== ホットプラグされたボリュームの制限

ホットプラグされたボリューム (VM の作成後に再起動なしでアタッチされたボリューム) を持つ VM はスナップショットを作成できません。ホットプラグされたボリュームを持つ VM のスナップショットを作成しようとすると、エージェントはエラーを報告し、解決手順を提案します。

. VM を停止して、ホットプラグされたボリュームをデタッチします。
. ボリュームがホットプラグされたものとみなされなくなるよう、VM spec 内でボリュームを永続化します。
. VM を再起動します。
. スナップショット操作を再試行します。

== Human-in-the-Loop の確認

4 つのスナップショットスキルはすべて、意図しないデータの変更を防ぐ安全モデルを実施します。

**読み取り専用操作** (`vm-snapshot-list`):

* 確認は不要です。エージェントはスナップショットを一覧表示し、すぐに結果を報告します。

**スナップショットの作成** (`vm-snapshot-create`):

* エージェントは、設定を提示する前にストレージバックエンドとゲストエージェントのステータスを分析します。
* 適用される正確な YAML が確認用に表示されます。
* スナップショットが作成される前に、"yes" で明示的に確認する必要があります。

**スナップショットの復元** (`vm-snapshot-restore`):

* 復元前に VM を停止する必要があります。実行中の場合、エージェントは停止を提案します。
* 確認のために正確なスナップショット名 (大文字と小文字を区別) を入力する必要があります。
* 入力による検証の後、もう一度 "yes" で確認する必要があります。
* 不一致または "yes" 以外の応答があった場合、操作はキャンセルされます。

**スナップショットの削除** (`vm-snapshot-delete`):

* エージェントはスナップショットの詳細を表示し、同じ VM で利用可能な他のスナップショットを一覧表示します。
* これがその VM の最後のスナップショットである場合、警告が表示されます。
* 確認のため、正確なスナップショット名 (大文字と小文字を区別) を入力する必要があります。
* 一致しない場合、操作はキャンセルされます。

== 一般的な問題

=== VolumeSnapshotClass が見つからない

エージェントが、お使いのストレージプロビジョナー用の `VolumeSnapshotClass` が存在しないと報告した場合、ストレージバックエンドがスナップショット用に設定されていない可能性があります。クラスター管理者に確認し、以下を実行して検証してください。

[source,bash,role=execute]
----
oc get volumesnapshotclass
----

=== クラッシュ整合性スナップショットの警告

エージェントがアプリケーション整合性ではなくクラッシュ整合性スナップショットを報告する場合、QEMU ゲストエージェントが VM 内で実行されていません。インストールして有効にしてください。

[source,bash,role=execute]
----
sudo dnf install qemu-guest-agent
sudo systemctl enable --now qemu-guest-agent
----

エージェントが接続されていることを確認します。

[source,bash,role=execute]
----
oc get vmi database-01 -n my-vms -o jsonpath='{.status.conditions[?(@.type=="AgentConnected")].status}'
----

----
True
----

=== 復元が In Progress のまま停止する

復元操作が 10 分以上 `InProgress` 状態のままになっている場合は、`VirtualMachineRestore` リソースでエラー状態を確認してください。

[source,bash,role=execute]
----
oc get virtualmachinerestore -n my-vms -o yaml
----

一般的な原因としては、ストレージクォータの不足、CSI ドライバーの問題、破損したスナップショットなどが考えられます。問題が解決しない場合は、クラスター管理者に問い合わせてください。

== クリーンアップ

このチュートリアルで作成したスナップショットを削除するには:

[source,bash,role=execute]
----
oc delete virtualmachinesnapshot --all -n my-vms
----

VM を削除するには:

[source,bash,role=execute]
----
oc delete vm database-01 -n my-vms
----

namespace を削除するには:

[source,bash,role=execute]
----
oc delete namespace my-vms
----

== まとめ

* `vm-snapshot-create` スキルは、ストレージバックエンドの完全な検証、ゲストエージェントの検出、実行前の確認を行って、特定時点のスナップショットを作成します。
* `vm-snapshot-list` スキルは、確認を必要とせずにスナップショットをステータス、作成からの経過時間、準備状態とともに表示する読み取り専用の操作です。
* `vm-snapshot-restore` スキルは最も厳格な安全モデルを適用します。リストアを実行する前に、VM が停止していること、スナップショット名を入力して確認すること、および最終的な yes/cancel による確認ゲートが必要です。
* `vm-snapshot-delete` スキルでは名前の入力による確認が必要であり、VM の最後のスナップショットを削除する際に警告が表示されます。
* アプリケーション整合性のあるスナップショットには、QEMU ゲストエージェント (Linux) または VSS サポート付きの VirtIO ドライバー (Windows) がインストールされ、実行されている必要があります。
* エンドツーエンドのアップグレード前ワークフローは、スナップショットの作成、アップグレードの実行、検証、そしてクリーンアップ (成功時) またはリストア (失敗時) のいずれかです。

== 参照

* xref:getting-started.adoc[Agentic VM 管理の開始]
* xref:natural-language-vm-create.adoc[自然言語による仮想マシンの作成]
* xref:ai-vm-lifecycle.adoc[AI を活用した VM ライフサイクル操作]
* xref:vm-clone.adoc[AI エージェントを使用した仮想マシンのクローン作成]
* link:https://github.com/RHEcosystemAppEng/agentic-plugins/tree/main/rh-virt[rh-virt スキルパックリポジトリ,window=_blank]
* link:https://github.com/openshift/openshift-mcp-server[OpenShift MCP Server,window=_blank]
* link:https://docs.openshift.com/container-platform/{ocp-docs-version}/virt/index.html[OpenShift Virtualization ドキュメント,window=_blank]
* link:https://docs.redhat.com/en/documentation/openshift_container_platform/{ocp-docs-version}/html-single/virtualization/index#virt-backup-restore-snapshots[OpenShift Virtualization スナップショット,window=_blank]
* link:https://kubevirt.io/user-guide/operations/snapshot_restore_api/[KubeVirt Snapshot and Restore API,window=_blank]
