= ストレージ設定
:navtitle:

== 概要

本モジュールでは、OpenShift Virtualization のストレージ設定について学習します。これには、LVM Storage Operator、HostPath Provisioner、ストレージの問題のトラブルシューティングが含まれます。

仮想マシン (VM) はディスクやデータ用に永続ストレージを必要とするため、適切なストレージ設定は VM のデプロイにおいて不可欠です。

== 学習目標

このセクションでは、以下の内容を学習します。

* ローカルディスクストレージ向けの LVM Storage Operator のインストールおよび設定方法
* シンプルなローカルストレージ向けの HostPath Provisioner の設定方法
* 自動プロビジョニングのための default StorageClass の設定方法
* 一般的なストレージの問題のトラブルシューティング方法
* OpenShift Virtualization におけるストレージ設定のベストプラクティス

== OpenShift Virtualization のストレージオプション

OpenShift Virtualization は、さまざまなストレージバックエンドをサポートしています。

* **LVM Storage**: Logical Volume Manager を使用したローカルディスクストレージ (本モジュールで解説)
* **HostPath Provisioner**: シンプルなローカルストレージのプロビジョニング (本モジュールで解説)
* **Ceph/ODF**: 分散ブロックストレージおよびファイルストレージ
* **NFS**: ネットワーク接続ストレージ
* **外部ストレージプロバイダー**: AWS EBS、Azure Disk など

本モジュールでは、Single Node OpenShift (SNO) やベアメタル環境に最適な、ローカルストレージのシナリオ向けの LVM Storage と HostPath Provisioner について解説します。

== 適切なストレージバックエンドの選択

以下の図を使用して、クラスターのトポロジーおよびワークロード要件に適したストレージバックエンドを決定してください。

image::storage-decision-diagram.png[Storage Decision Diagram, align=center]

この決定は、クラスターのトポロジー (シングルノードかマルチノードか)、ライブマイグレーションが必要かどうか (RWX アクセスモードが必要)、およびどのような既存のストレージインフラストラクチャーが利用可能であるかという 3 つの要因に依存します。以下の比較表は、各バックエンドの機能をまとめたものです。

[cols="1,1,1,1,1,1,1,2",options="header"]
|===
|ストレージバックエンド |アクセスモード |ボリュームモード |スナップショット |ライブマイグレーション |シンプロビジョニング |パフォーマンス |ユースケース

|LVM Storage
|RWO
|Block
|はい
|いいえ (RWO のみ)
|はい
|高 (ローカルディスク)
|ローカルディスクを備えたシングルノードまたはベアメタル実稼働環境

|HostPath Provisioner
|RWO
|Filesystem
|いいえ
|いいえ (RWO のみ)
|いいえ
|中程度
|開発/テスト、プロトタイピング、エッジ、シングルノード

|ODF/Ceph (RBD)
|RWO、RWX
|Block
|はい
|はい (RWX マルチアタッチ)
|はい
|高 (分散)
|マルチノード実稼働環境、ライブマイグレーションを伴う VM ワークロードに推奨

|ODF/Ceph (CephFS)
|RWO、RWX
|Filesystem
|はい
|はい (RWX)
|はい
|高 (分散)
|ライブマイグレーションを伴うマルチノード実稼働環境

|NFS
|RWO、RWX
|Filesystem
|いいえ
|はい (RWX)
|状況による
|中程度 (ネットワーク)
|既存の NFS インフラストラクチャーを備えた共有ストレージ

|外部 CSI
|状況による
|状況による
|状況による
|状況による
|状況による
|状況による
|クラウドデプロイメント (AWS EBS、Azure Disk など)
|===

NOTE: ライブマイグレーションでは、マイグレーション中に移行元ノードと移行先ノードの両方から VM ディスクにアクセスできる必要があります。ODF はこれを 2 つの方法でサポートしています。複数のノードからの同時アクセスを可能にする Block モードの Ceph RBD (I/O パフォーマンスが高いため、VM ワークロードに推奨される RWX マルチアタッチ) と、共有ファイルシステムアクセスを提供する Filesystem モードの CephFS (RWX) です。NFS も RWX アクセスを提供します。ライブマイグレーションを必要とする実稼働クラスターには、Ceph RBD を備えた ODF が推奨されるソリューションです。

== 前提条件

このセクションのチュートリアルを進める前に、以下が用意されていることを確認してください。

* OpenShift 4.18 以降のクラスター
* クラスター管理者権限
* クラスターノード上の少なくとも 1 つの予備ローカルディスク
* Linux のストレージ概念 (LVM、ブロックデバイス) に関する基本的な知識

== 各セクション

xref:lvm-operator.adoc[]::
ローカルディスクを使用して LVM Storage Operator をインストールおよび設定するためのステップバイステップガイドです。

xref:lvm-troubleshooting.adoc[]::
LVM ストレージの問題を診断および解決するためのトラブルシューティングガイドです。

xref:hostpath-provisioner.adoc[]::
開発、テスト、シングルノードクラスターのシナリオ向けに、HostPath Provisioner を使用したローカルストレージの設定ガイドです。
xref:multi-disk-configuration.adoc[]::
異なる StorageClass を使用して複数のディスクを持つ VM の設定ガイドです。

== デフォルト Storage Class の重要性

デフォルト Storage Class の設定は、OpenShift Virtualization にとって非常に重要です。

* **ブートソースイメージ**: DataSource OS イメージにはデフォルト Storage Class が必要です
* **自動プロビジョニング**: ストレージを明示的に指定しなくても VM を作成できます
* **テンプレートのサポート**: VM テンプレートがすぐに使用できます
* **運用管理の簡素化**: クラスター全体で一貫したストレージプロビジョニングが可能になります

詳細については、LVM Operator ガイドの xref:lvm-operator.adoc#_step_5_set_default_storageclass[Step 5: Set Default StorageClass] を参照してください。

== 次のステップ

このモジュールを完了すると、以下の準備が整います。

* VM の xref:networking:index.adoc[ネットワークの設定]
* 仮想マシンの作成と設定

== 関連情報

* xref:getting-started:default-storage-class.adoc[デフォルト Storage Class の設定]
* link:https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html/storage/persistent-storage-using-local-storage#persistent-storage-using-lvms[OpenShift LVM Storage ドキュメント,window=_blank]